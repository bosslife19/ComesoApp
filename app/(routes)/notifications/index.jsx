import React from 'react';
import { StyleSheet, View } from 'react-native';
import NotificationTable from '../../../screens/NotificationTable/NotificationTable';

const Notifications = () => {
    return (
        <View>
            <NotificationTable/>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Notifications;

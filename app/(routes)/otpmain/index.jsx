import React from 'react';
import { StyleSheet, View } from 'react-native';
import OTPVerifies from '../../../screens/auth/OTP/OTPVerifies';

const OTPVerify = () => {
    return (
         <OTPVerifies/>
     );
}

const styles = StyleSheet.create({})

export default OTPVerify;
